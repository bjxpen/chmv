import { type ChmSystemInfo } from './system.js';
import { type DecodedText } from './text.js';
import { type ChmToc } from './toc.js';
export interface ArchiveInfo {
    system: ChmSystemInfo | null;
    counts: {
        files: number;
        directories: number;
    };
}
export interface ArchiveListEntry {
    path: string;
    start: string;
    length: string;
    space: 'compressed' | 'uncompressed';
    flags: number;
}
export interface ArchiveSitemapResult {
    path: string;
    toc: ChmToc;
}
export interface ArchiveCatResult extends DecodedText {
    path: string;
}
export interface ArchiveManifest {
    system: ChmSystemInfo | null;
    counts: {
        files: number;
        directories: number;
    };
    defaultTopic: string | null;
    tocFile: string | null;
    indexFile: string | null;
    files: string[];
}
export interface ExtractArchiveOptions {
    only?: 'html' | 'assets';
    excludeMeta?: boolean;
}
export interface ExtractedArchiveFile {
    path: string;
    outPath: string;
}
export declare function getArchiveInfo(filePath: string): Promise<ArchiveInfo>;
export declare function listArchiveEntries(filePath: string, dir?: string): Promise<ArchiveListEntry[]>;
export declare function loadArchiveToc(filePath: string): Promise<ArchiveSitemapResult>;
export declare function loadArchiveIndex(filePath: string): Promise<ArchiveSitemapResult>;
export declare function catArchiveText(filePath: string, entryPath: string, options?: {
    charset?: string;
}): Promise<ArchiveCatResult>;
export declare function getArchiveManifest(filePath: string): Promise<ArchiveManifest>;
export declare function extractArchive(filePath: string, outdir: string, options?: ExtractArchiveOptions): Promise<ExtractedArchiveFile[]>;
//# sourceMappingURL=cli-commands.d.ts.map