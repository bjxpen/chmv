import type { ChmFileKind } from "./types.js";
/** Normalize CHM internal paths to a canonical POSIX form. */
export declare function normalizeChmPath(path: string): string;
/** Get lowercase extension including the dot. */
export declare function getExtension(path: string): string;
/** Classify a CHM entry path into a file kind. */
export declare function classifyChmPath(path: string, isDirectory: boolean): ChmFileKind;
/** Convert a CHM topic path to a markdown route (no extension). */
export declare function chmTopicToRoute(topicPath: string): string;
/** Convert a route to a markdown file path relative to project root. */
export declare function routeToMarkdownPath(route: string): string;
/** Sanitize a project-relative path for the host filesystem. */
export declare function toDiskPath(projectPath: string): string;
/** Join path segments without duplicate slashes. */
export declare function joinPosix(...parts: string[]): string;
/** Resolve a relative href against a CHM topic path. */
export declare function resolveChmHref(baseTopic: string, href: string): string;
//# sourceMappingURL=paths.d.ts.map