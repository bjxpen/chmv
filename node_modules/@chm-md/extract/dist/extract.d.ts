import type { ChmBundle, ProgressHandler } from "@chm-md/shared";
import { countTocNodes, maxTocDepth, parseBinaryToc } from "./binary-toc.js";
import { countSitemapNodes, maxSitemapDepth, parseSitemap } from "./sitemap.js";
export interface ExtractChmOptions {
    sourcePath: string;
    outputDir?: string;
    preferBinaryToc?: boolean;
    preferBinaryIndex?: boolean;
    onProgress?: ProgressHandler;
}
export interface ExtractChmResult {
    bundle: ChmBundle;
    files: Map<string, Uint8Array>;
}
/** Extract a CHM archive into a ChmBundle and file map. */
export declare function extractChm(options: ExtractChmOptions): Promise<ExtractChmResult>;
export { decodeChmText } from "./decode.js";
export { countSitemapNodes, countTocNodes, maxSitemapDepth, maxTocDepth, parseBinaryToc, parseSitemap, };
//# sourceMappingURL=extract.d.ts.map