import type { MarkdownProject } from "@chm-md/shared";
export interface LoadedProject extends MarkdownProject {
    projectDir: string;
}
/** Load a MarkdownProject IR from a project directory on disk. */
export declare function loadProject(projectDir: string): Promise<LoadedProject>;
//# sourceMappingURL=load-project.d.ts.map