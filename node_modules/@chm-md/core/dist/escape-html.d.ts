/** Escape markdown-sensitive characters in prose while preserving code, HTML, and real links. */
export declare function escapeMarkdownProse(markdown: string): string;
/** @deprecated Prefer escapeMarkdownProse. */
export declare function escapeAngleBracketsInProse(markdown: string): string;
type SegmentType = "prose" | "fenced-code" | "inline-code" | "html-block";
export interface MarkdownSegment {
    type: SegmentType;
    content: string;
}
/** Split markdown into prose, code, and preserved HTML regions. */
export declare function splitMarkdownSegments(markdown: string): MarkdownSegment[];
export {};
//# sourceMappingURL=escape-html.d.ts.map