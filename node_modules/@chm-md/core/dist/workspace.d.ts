import type { DocsWorkspace, MarkdownProject, ResolvedWorkspace } from "@chm-md/shared";
import type { ConvertChmOptions } from "./convert-chm.js";
declare const WORKSPACE_MANIFEST_NAME = "docs-workspace.json";
export interface ResolveWorkspaceOptions extends Omit<ConvertChmOptions, "sourcePath" | "outputDir"> {
    manifestPath: string;
    stagingDir?: string;
}
/** Resolve workspace manifest path from a file or directory target. */
export declare function resolveWorkspaceManifestPath(target: string): string | null;
/** True when target is a MarkdownProject directory. */
export declare function isMarkdownProjectDir(target: string): boolean;
/** Load and validate a docs workspace manifest. */
export declare function loadWorkspaceManifest(manifestPath: string): Promise<DocsWorkspace>;
/** Apply route prefix to a loaded MarkdownProject. */
export declare function prefixProject(project: MarkdownProject, prefix: string): MarkdownProject;
/** Convert CHM entries in a workspace to MarkdownProjects under stagingDir. */
export declare function convertWorkspace(manifestPath: string, stagingDir: string, options?: Omit<ConvertChmOptions, "sourcePath" | "outputDir">): Promise<DocsWorkspace>;
/** Resolve all collections in a workspace to prefixed MarkdownProjects. */
export declare function resolveWorkspace(options: ResolveWorkspaceOptions): Promise<ResolvedWorkspace>;
export { WORKSPACE_MANIFEST_NAME };
//# sourceMappingURL=workspace.d.ts.map