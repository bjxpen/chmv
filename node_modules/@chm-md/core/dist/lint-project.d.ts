import type { Configuration } from "markdownlint";
import type { WarningSummary } from "@chm-md/shared";
export interface LintProjectSummary {
    projectDir: string;
    pageCount: number;
    fixedCount: number;
    durationMs: number;
    warnings: WarningSummary;
}
/** Re-lint (and optionally fix) all pages in a MarkdownProject directory. */
export declare function lintProject(options: {
    projectDir: string;
    configPath?: string;
    config?: Configuration;
    fix?: boolean;
}): Promise<LintProjectSummary>;
export { defaultStyleProfile } from "./style-profile.js";
//# sourceMappingURL=lint-project.d.ts.map