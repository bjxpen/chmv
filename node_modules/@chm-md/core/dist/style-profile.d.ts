import type { Configuration } from "markdownlint";
export interface MarkdownStyleProfile {
    headingStyle: "atx" | "setext";
    bulletListMarker: "-" | "*" | "+";
    emDelimiter: "*" | "_";
    strongDelimiter: "**" | "__";
    codeBlockStyle: "fenced" | "indented";
    expandTabs: boolean;
    tabWidth: number;
    trimTrailingWhitespace: boolean;
    ensureFinalNewline: boolean;
    demoteDuplicateH1: boolean;
    ensureBlankLineBeforeHeadings: boolean;
    collapseExcessBlankLines: boolean;
}
/** Built-in CHM-friendly markdownlint defaults. */
export declare function defaultLintConfig(): Configuration;
/** Derive Turndown/post-process options from a markdownlint configuration. */
export declare function buildStyleProfile(config: Configuration): MarkdownStyleProfile;
/** Default style profile when lint is disabled. */
export declare function defaultStyleProfile(): MarkdownStyleProfile;
//# sourceMappingURL=style-profile.d.ts.map