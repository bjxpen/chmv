import type { MarkdownProject } from "@chm-md/shared";
export interface LoadMarkdownCollectionOptions {
    sourceDir: string;
    title: string;
}
/** True when a directory contains markdown files but no MarkdownProject manifest. */
export declare function isMarkdownDocsDir(target: string): Promise<boolean>;
/** Load a plain markdown folder as an in-memory MarkdownProject. */
export declare function loadMarkdownCollection(options: LoadMarkdownCollectionOptions): Promise<MarkdownProject>;
//# sourceMappingURL=load-markdown-collection.d.ts.map